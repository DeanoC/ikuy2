package ikuy

import ikuy.common._
/*
class SystemOnChip( override val chipName : String, 
                    override val isHard : Boolean,
                    val chips : List[Chip], 
                    val buses : List[Bus],
                    val interconnects : List[Interconnect])
extends Chip { 
  override val chipType = "SystemOnChip" 

  override def use(): Unit = {
    chips.foreach(_.use )
    buses.foreach(_.use )
    interconnects.foreach(_.use)
  }
}
*/